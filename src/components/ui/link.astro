---
import type { HTMLAttributes } from 'astro/types';
import { twMerge } from 'tailwind-merge';
import type { VariantProps } from 'tailwind-variants';
import Arrow from './arrow.astro';
import Icon from './icon.astro';
import Text, { textVariants } from './text.astro';

type Props = HTMLAttributes<'a'> & VariantProps<typeof textVariants> & { icon?: string };

const { class: className, href, target, weight, icon, ...props } = Astro.props;
const external = href?.toString().startsWith('https://');
---

<Text
  {...props}
  as="a"
  href={href}
  target={external ? '_blank' : target}
  weight={weight ?? 'semibold'}
  class={twMerge([
    'group inline-flex items-center justify-center rounded text-accent-11 motion-safe:transition-colors',
    'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-8',
    external && 'italic',
    className,
  ])}
>
  {icon ? <Icon name={icon} /> : <slot />}
  {external && !icon && <Arrow name="up-right" />}
</Text>
